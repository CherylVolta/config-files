# 请将该 Dockerfile 放在后端项目的根目录下
FROM tomcat:10-jdk21-temurin
LABEL authors="Oddy"

# 后端项目构建出的的 war 包的相对路径，根据情况修改，这里以 target/your-project-name-*.war 为例
COPY target/your-project-name-*.war /usr/local/tomcat/webapps/
EXPOSE 8080

ENTRYPOINT ["catalina.sh", "run"]
